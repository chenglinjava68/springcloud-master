spring:
  application: 
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/chenglinjava68/springcloud-master.git  # prefix
          search-paths: config #相对路径
          
    
server: 
  context-path: /
  port: 4000
  
  